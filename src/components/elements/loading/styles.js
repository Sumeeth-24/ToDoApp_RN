import {StyleSheet} from 'react-native';

const styles = () =>
  StyleSheet.create({
    container: {
      position: 'absolute',
    },
    spinnerTextStyle: {
      color: '#fff',
    },
  });

export default styles;
